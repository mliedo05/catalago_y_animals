<%= form_with(model: payment, local: true) do |form| %>
  <% if payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(payment.errors.count, "error") %> prohibited this payment from being saved:</h2>

      <ul>
      <% payment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :order_id %>
    <%= form.collection_select(:order_id, @orders, :id, :name) %>
  </div>

  <div class="field">
    <%= form.label :paymentable_id %>
    <%= form.collection_select(:paymentable_id, @payments, :id, :paymentable_type) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
